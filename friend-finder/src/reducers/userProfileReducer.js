

const initialState = {
  loading: false,
  users: [],
  error: null
}

export const userProfileReducer = (state = initialState, action) => {
  switch(action.type) {
    default: 
      return state;
  }
}